const initialState = {
  isActive: false,
  hangTime: 7,
};

function timers_reducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}

export default timers_reducer;
